# typescript-eslint@8.x requires typescript <6.0.0 but this project uses TypeScript 6.
# Legacy peer deps allows npm ci to install without failing on this conflict.
# Remove once typescript-eslint releases official TypeScript 6 support.
legacy-peer-deps=true
